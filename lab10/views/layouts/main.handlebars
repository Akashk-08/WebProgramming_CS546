<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{title}}</title>
    <link rel="stylesheet" href="/public/css/sitestyles.css">
  </head>

  {{!-- Safely apply background and font color ONLY if themePreference exists --}}
  <body 
    {{#if themePreference}}
      style="background-color: {{themePreference.backgroundColor}}; color: {{themePreference.fontColor}};"
    {{/if}}
  >

    <header>
      {{#if header}}
        <h1>{{header}}</h1>
      {{/if}}
    </header>

    <main>
      {{{body}}}
    </main>

    <footer>
      <p>Created by Akash Kulkarni, CWID: 20034382</p> 
    </footer>

    <script src="/public/js/form_validate.js"></script>
  </body>
</html>
